// Ejercicio 176: Iterar en orden los elementos de un arreglo con el ciclo for.

let lenguajes = ['JS', 'PHP', 'C', 'Java', 'Python'];

for(let i = 0; i < lenguajes.length; ++i){
    console.log(lenguajes[i]);
}
