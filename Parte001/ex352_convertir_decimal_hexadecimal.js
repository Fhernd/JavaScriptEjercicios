// Ejercicio 352: Convertir un número decimal en un número hexadecimal.

// Número en base 10:
let decimal = 512;

// Número en base 16:
let resultado = decimal.toString(16);

console.log(decimal);
console.log(resultado);
