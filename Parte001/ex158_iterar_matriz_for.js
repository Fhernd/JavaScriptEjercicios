// Ejercicio 158: Iterar el contenido de una matriz (arreglo bidimensional).

// Solución:

let numeros = new Array([1, 2, 3], [4, 5, 6], [7, 8, 9]);

console.log(numeros.length);
console.log(numeros);

console.log();

for(let i = 0; i < numeros.length; ++i){
    console.log(numeros[i]);
}
