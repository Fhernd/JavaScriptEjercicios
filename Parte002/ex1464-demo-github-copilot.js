// Ejercicio 1464: Demostración Básica de GitHub Copilot.

// Función para sumar dos números enteros:

function sumar(a, b) {
    return a + b;
}

// Function to tweet a custom message using an API:
function tweet(message) {
    // ...
}

// Create a class to represent an employee:
class Employee {
    constructor(name, title) {
        this.name = name;
        this.title = title;
    }
}

// Detect the load event:
window.addEventListener('load', function() {
    // Display a message in the console:
    console.log('The page has loaded.');

    // Mostrar otro mensaje en español en la consola:
    console.log('La página ha cargado.');
});
